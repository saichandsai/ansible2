---
- hosts: ubuntuserver
  become: yes
  vars:
    var1: aws
    var2: cloud
    vars: linux
  tasks:
   - name: display var1
     debug:
      msg: "{{var1}}"
   - name: display var2
     debug:
      msg: "{{var2}}"
